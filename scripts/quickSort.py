#!/usr/bin/python


def quickSort(args):
    less[]
    pivot[]

    if len(args) >1:
        pivot_index= len(ags)/2
        small_items=[]
        large_items=[]
        for i , val in enumerate(args):

            if i != pivot_index:
                if val < args[pivot_index]:
                    small_items.append(val)
                else:
                    large_items.append(val)
         qucikSort(samll_items)
         quickSort(large_items)
         items[:] = small_items + [args[pivot_index]]+ large_items



